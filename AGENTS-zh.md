# AGENTS.md 中文说明

本文档是根目录 [AGENTS.md](AGENTS.md) 的中文说明版，帮助维护者理解 Agent 在本仓库中的工作方式。

实际约束以 `AGENTS.md` 为准。`AGENTS-zh.md` 主要供人阅读，不应假定工具会自动把它当作 Agent 指令加载。修改规则时，应同步维护两个文件，避免含义不一致。

## 一、仓库定位

本仓库是研究开源 Agent 系统的长期知识库，用于保存：

- 架构分析；
- 核心实现机制；
- 项目间横向比较；
- 可复现实验；
- 可编辑架构图。

被研究的源码仓库作为独立 Git 仓库放在 `<workspace>/agent/` 下，与 `agent-systems-atlas` 保持同级。

最重要的规则是：

> 源码仓库用于阅读、运行和隔离实验；研究成果统一保存在 `agent-systems-atlas`。

这种物理隔离可以防止笔记被误提交到上游仓库，也降低源码工作区清理导致笔记丢失的风险。

## 二、目录如何分工

- `projects/<project>/`：分析某一个具体 Agent 项目。
- `patterns/`：提炼由多个项目共同支持的设计模式。
- `comparisons/`：围绕一个边界明确的问题进行横向比较。
- `experiments/`：保存可复现的运行观察和实验产物。
- `concepts/`：记录术语、概念和个人定义。
- `templates/`：提供分析文档与实验记录模板。
- `assets/`：保存多个文档共享的附件。
- `scripts/`：提供轻量的仓库维护工具。

Atlas 不引入 `sources/`，也不复制或嵌入完整的上游源码。分析时直接读取同级源码仓库。

## 三、Agent 接到研究任务后的工作机制

一次标准的源码研究任务按以下顺序推进。

### 1. 确定分析对象

先确认：

- 上游源码仓库；
- 当前 Git commit；
- 本次要回答的具体问题；
- 结论应写入 `projects/`、`patterns/`、`comparisons/` 还是 `experiments/`。

commit 应写入 `projects/<project>/project.yaml`；如果任务包含实验，实验记录中也要单独保存 commit。

### 2. 建立源码证据

Agent 必须阅读实际实现和相关测试，不能只根据文件名、类型名或项目文档推测运行机制。

重要结论应能追溯到：

- 入口文件；
- 核心实现；
- 关键数据结构；
- 相关测试；
- 对应源码版本。

### 3. 区分信息性质

文档中要明确区分：

- **代码事实**：源码可以直接证明的行为；
- **解释**：对设计意图或权衡的分析；
- **实验观察**：实际运行得到的结果；
- **未确认事项**：尚缺少证据的判断。

源码阅读不能冒充运行验证。没有执行实验时，结论应明确标注为静态分析结果。

### 4. 组织研究成果

优先围绕具体机制写作，例如：

- Agent Loop 如何推进和终止；
- 上下文如何构建、压缩与恢复；
- 工具如何注册、审批和执行；
- Session 如何恢复；
- 工作空间如何隔离。

如果可以比较具体机制，就不创建宽泛的“项目 A 对比项目 B”文档。

只有当至少两个项目的实现或实验支持同一规律时，才将其提炼到 `patterns/`。只有一个实现时，应保留在项目分析中，并说明它是否可能具有普遍性尚未确认。

### 5. 验证并交付

文档修改完成后，Agent 要检查格式、链接、元数据和生成文件，然后向用户说明：

- 新增或修改了哪些笔记；
- 分析基于哪些上游 commit；
- 新增了哪些图或实验产物；
- 哪些结论仍未验证。

除非用户明确要求，否则 Agent 不自行提交或推送 Git 变更。

## 四、源码仓库的安全边界

- 不把唯一一份研究笔记保存在上游源码仓库中。
- 未经用户明确要求，不移动或删除源码仓库中的原始 `notes/`。
- 即使用户要求迁移，也应先确认 Atlas 副本内容一致并已进入 Git 历史。
- 除非任务明确要求源码实验或修改，否则把同级源码仓库视为只读。
- 不为 Atlas 集成修改上游仓库的 `.gitignore`。
- 需要 `.analysis` 快捷入口时，使用 `scripts/link-project.sh`。
- `.analysis` 通过源码仓库本地的 `.git/info/exclude` 排除。
- 不把 `.analysis`、Atlas 笔记或研究产物提交到上游仓库。

## 五、架构图如何生成

所有新建或大幅修改的架构图必须使用 `excalidraw-diagram-generator` skill 构建。

必须满足：

- 保留可编辑的 `.excalidraw` 源文件；
- 项目专属图放在 `projects/<project>/diagrams/`；
- 跨项目图放在对应主题旁的 `diagrams/` 目录；
- 文件名使用描述性的 kebab-case，例如 `agent-loop-runtime.excalidraw`；
- 说明该架构的 Markdown 文档应链接 `.excalidraw` 文件；
- PNG 或 SVG 可以作为预览，但不能代替 `.excalidraw` 源文件。

架构图应清楚表达：

- 组件边界；
- 控制流或数据流方向；
- 颜色和线型的含义；
- 无需依赖正文也能理解的简短标签。

Mermaid、ASCII 图、截图和生成的位图不能替代正式的 Excalidraw 架构图。普通文字中的小型流程表达不属于正式架构图，可以继续使用。

如果 `excalidraw-diagram-generator` 不可用，Agent 应暂停绘图部分并说明 skill 需要安装或启用，不能虚构调用方式，也不能静默改用其他格式。

## 六、写作规范

- 默认使用中文，除非所在文档已经采用其他语言。
- 先给结论，再说明证据和机制。
- 仓库内文档使用相对 Markdown 链接。
- 代码符号、命令、文件路径和 commit 使用反引号。
- 使用“未确认事项”或 `Uncertainties` 明确保留不确定性。
- 项目分析、模式和比较文档之间避免重复解释，优先链接到权威版本。
- 不为套用新模板而机械重写已经迁入的笔记；只有开展实质分析时才修改。
- 不在可移植文档和元数据中写入个人机器的绝对路径。

## 七、项目元数据

每个项目至少包含一个 `projects/<project>/project.yaml`：

```yaml
name: project-name
repository: https://example.com/owner/repository
analyzed_commit: full-git-commit-sha
last_reviewed: YYYY-MM-DD
```

只与本机有关的路径写入 `*.local.yaml`。此类文件由 Git 忽略，不应进入版本控制。

## 八、实验记录

每个实验至少记录：

- 要验证的问题；
- 上游仓库和准确 commit；
- 相关环境与配置；
- 可复现步骤；
- 与结论分开的实际观察；
- 不确定性、失败信息和保留的产物。

实验的作用是验证系统实际如何工作，而不是重复静态源码分析。

## 九、修改后的检查清单

对于只修改文档和研究产物的任务，Agent 至少执行：

1. 运行 `git diff --check`；
2. 确认新增的相对链接和引用文件存在；
3. 检查 `.code-workspace` JSON 是否有效；
4. 检查修改过的 `.excalidraw` JSON 是否有效；
5. 迁移笔记时，比对 Atlas 文件与原文件内容；
6. 在提出删除原文件前，确认迁移副本已经验证。

## 十、整体执行链路

```text
确认问题与源码版本
        ↓
阅读实现和相关测试
        ↓
建立结论与源码位置的映射
        ↓
区分事实、解释、实验和不确定性
        ↓
写入项目分析、模式、比较或实验目录
        ↓
使用 Excalidraw skill 生成可编辑架构图（如需要）
        ↓
检查格式、链接、JSON 和迁移完整性
        ↓
汇报变更、证据和未验证事项
```

这套机制的目标是让每一条分析结论都具备三个属性：可追溯、可复现、可长期维护。
